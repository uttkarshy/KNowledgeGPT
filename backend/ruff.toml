target-version = "py312"
line-length = 120

[lint]
select = ["E4", "E7", "E9", "F", "I", "B"]

[lint.flake8-bugbear]
# FastAPI's dependency/parameter markers are immutable framework declarations.
extend-immutable-calls = ["fastapi.Depends", "fastapi.Query"]
